# Leave empty when a reverse proxy exposes the backend under the same origin.
# For a separate API, set its URL here and add this frontend origin to
# the backend FRONTEND_ORIGINS environment variable.
VITE_API_BASE_URL=
